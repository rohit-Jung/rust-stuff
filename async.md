- futures => promises

- event loop; callback queue (js runtime) | rust doesn't have async runtime
- async vs multithreading ( rust has both )
- tokio makes async code in rust easy ( Actix, Poem )

- axiom or poem { similar to express ? easy for openapi spec }

- poem uses dependency injection { adding store to params ? }
- disel or sqlx
